# Claude AI (required for cover letter tailoring)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# SMTP credentials (if using smtp provider)
SMTP_USERNAME=your@email.com
SMTP_PASSWORD=your-app-password-here

# SendGrid (if using sendgrid provider for bulk sending)
SENDGRID_API_KEY=SG.your-key-here
